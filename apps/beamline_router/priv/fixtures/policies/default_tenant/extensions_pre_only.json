{
  "version": "1.0",
  "providers": [
    {"name": "openai", "weight": 100}
  ],
  "pre": [
    {
      "id": "normalize_text",
      "mode": "required",
      "config": {
        "lowercase": true
      }
    },
    {
      "id": "add_metadata",
      "mode": "optional"
    }
  ],
  "metadata": {
    "strategy": "extensions_pre_only",
    "description": "Only pre-processor extensions"
  }
}

