## Acceptance Criteria

1. A single documented script or command exists to run Batch #1.
2. Batch #1 runs from scratch without manual resume.
3. All Batch #1 suites:
   - fast pass (or 0 tests if heavy-only)
   - full pass (or 0 tests if heavy-only)
   - heavy pass
4. progress.md contains final, factual results for all Batch #1 suites.
5. No timeouts or ETS errors occur in the clean run.
