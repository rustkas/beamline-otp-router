Scope:
- Validate router_caf_adapter_SUITE and router_caf_adapter_enhanced_SUITE exist and are consistent with the reported fixes.
- Confirm router_jetstream_recovery_ext_SUITE stays intact with required callbacks.
- Run the dedicated verification steps (linter, ct-full, targeted suite run) and record results.
