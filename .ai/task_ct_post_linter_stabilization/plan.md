1. Discover the relevant suites (`router_caf_adapter_SUITE`, `router_caf_adapter_enhanced_SUITE`, `router_jetstream_recovery_ext_SUITE`) and record their status.
2. Run the suite linter command and log results in progress.
3. Execute `./scripts/ct-full.sh` and capture the output summary.
4. Run the targeted `router_idem_core_SUITE` and validate it passes.
5. Ensure progress.md reflects each step and any follow-up fixes if needed.
