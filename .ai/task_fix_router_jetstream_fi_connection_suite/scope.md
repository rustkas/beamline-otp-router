# Scope

## In Scope
- `test/router_jetstream_fi_connection_SUITE.erl`
- Helper modules used by this suite (if strictly necessary for test setup).
- Run and analysis of `rebar3 ct --suite router_jetstream_fi_connection_SUITE`.

## Out of Scope
- Other test suites (unless for regression checking).
- Production code features (unless strictly correctness fixes).
- CI configuration changes.
