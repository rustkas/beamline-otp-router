%% Common Test configuration for beamline_router
%% Enables gRPC during tests and keeps NATS in mock mode
[
  {beamline_router, [
    {grpc_enabled, true},
    {grpc_port, 50051},
    {nats_mode, mock}
  ]}
].
